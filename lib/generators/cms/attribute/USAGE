Description:
    Creates a CMS migration for a new CMS attribute. Per default the attribute type is set to
    "string". The "values" option is only available for "enum" or "multi-enum" types.

Example:
    `rails generate cms:attribute show_in_navigation --type=enum --values=yes no`

    Migration: cms/migrate/123456_create_show_in_navigation_attribute.rb