require 'spec_helper'

describe LanguageSwitchCell do
  let(:homepage) { mock_model(Homepage, permalink: 'homepage') }

  before do
  end

  it 'inherits from Cell::Rails' do
    #subject.should be_a(Cell::Rails)
  end

end